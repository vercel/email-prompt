{
  "name": "email-prompt",
  "version": "0.4.0",
  "repository": "vercel/email-prompt",
  "license": "MIT",
  "files": [
    "build.js"
  ],
  "main": "build.js",
  "description": "CLI email prompt with autocompletion and built-in validation",
  "scripts": {
    "build": "buble -y dangerousForOf index.js > build.js",
    "prepublish": "npm run build"
  },
  "dependencies": {
    "ansi-escapes": "2.0.0",
    "chalk": "1.1.3"
  },
  "devDependencies": {
    "buble": "0.15.2"
  }
}
